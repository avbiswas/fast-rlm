{
  "name": "mcp-testbed",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "license": "MIT",
  "description": "A tiny MCP server (tools + resources + prompts) runnable over stdio and Streamable HTTP, used to test fast-rlm MCP integration.",
  "scripts": {
    "stdio": "bun run stdio.ts",
    "http": "bun run http.ts",
    "client": "bun run client.ts",
    "fsio-stdio": "bun run fsio-stdio.ts",
    "fsio-http": "bun run fsio-http.ts",
    "fsio-client": "bun run fsio-client.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@modelcontextprotocol/server-filesystem": "^2026.1.14",
    "express": "^4.21.2",
    "html-to-markdown-mcp": "^1.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^4.17.21"
  }
}
